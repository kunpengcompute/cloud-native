[pytest]
# BDD markers for qos operator e2e
markers =
    e2e: end-to-end tests
    smoke: smoke tests
    lifecycle: policy lifecycle tests
    resctrl: resctrl state assertion tests
    negative: negative validation tests

# Keep output readable for diagnose-on-failure logs.
addopts = -v --tb=short -s
