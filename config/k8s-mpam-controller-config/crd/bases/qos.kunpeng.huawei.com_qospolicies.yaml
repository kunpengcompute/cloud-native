---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: qospolicies.qos.kunpeng.huawei.com
spec:
  group: qos.kunpeng.huawei.com
  names:
    kind: QoSPolicy
    listKind: QoSPolicyList
    plural: qospolicies
    shortNames:
    - qos
    singular: qospolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mb.pri
      name: MBPRI
      type: integer
    - jsonPath: .spec.l3.ways
      name: L3WAYS
      type: integer
    - jsonPath: .spec.cpu.qosLevel
      name: CPUQOS
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: QoSPolicy is the Schema for QoS policy CRD.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QoSPolicySpec defines the desired state for QoS settings.
            properties:
              cpu:
                default: {}
                description: CPU groups cpu-related controls.
                properties:
                  qosLevel:
                    default: 0
                    description: |-
                      QoSLevel controls cpu.qos_level for pods in this policy group.
                      Current supported range is [-1, 1].
                    format: int32
                    maximum: 1
                    minimum: -1
                    type: integer
                type: object
              l3:
                default: {}
                description: L3 groups cache related policy items.
                properties:
                  max:
                    default: 100
                    description: MAX range is [0, 100]. Default is 100.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  min:
                    default: 0
                    description: MIN range is [0, 100]. Default is 0.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  pri:
                    default: 0
                    description: PRI range is [0, 3]. Default is 0.
                    format: int32
                    maximum: 3
                    minimum: 0
                    type: integer
                  ways:
                    description: |-
                      Ways is the number of cache ways to allocate. Minimum is 1.
                      Machine-specific upper bound should be checked by the controller on each node.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              mb:
                default: {}
                description: MB groups memory-bandwidth related policy items.
                properties:
                  hdl:
                    default: 1
                    description: HDL can only be 0 or 1. Default is 1.
                    format: int32
                    maximum: 1
                    minimum: 0
                    type: integer
                  max:
                    default: 100
                    description: MAX range is [0, 100]. Default is 100.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  min:
                    default: 0
                    description: MIN range is [0, 100]. Default is 0.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  pri:
                    default: 3
                    description: PRI range is [0, 7]. Default is 3.
                    format: int32
                    maximum: 7
                    minimum: 0
                    type: integer
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector selects which nodes this policy applies to.
                  If omitted, the controller should decide default behavior (typically all nodes).
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
